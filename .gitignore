# secrets / env
.env
.env.*
*.key
*.pem
*-sa.json
service-account*.json
# python
__pycache__/
*.py[codz]
.venv/
venv/
# node
node_modules/
# IDE
.idea/
.vscode/
# OS
.DS_Store
Thumbs.db
# 요약 입력(에이전트 전용, 영문 본문 — 커밋 안 함)
docs/data/summary_input.json

# Cladding runtime state
.cladding/

# 파이프라인 내부 상태(dedup 원장 등) — 공개 배포 제외, 매 실행 diff가 커서 커밋 부적합
state/
